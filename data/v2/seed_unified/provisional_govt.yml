# V2 seed_unified for political entity `provisional_govt`.
# Generated 2026-08-25 by scripts/maintenance/merge_seeds_cross_source.py
# from data/v2/seed/<source>/provisional_govt.yml across all sources.
#
# Input: 0 per-source seed entries.
# Output: 0 unified entries (one per physical coin).
#
# Each unified entry's `composed_of` lists the per-source seeds
# it absorbed via the §5.2 match hierarchy. `weight_rough_g[]`,
# `fineness[]`, `diameter_mm[]` are multi-source lists preserving
# every reading per CLAUDE.md §9a. `sources[]` is the union.

id: provisional_govt
generated_at: '2026-08-25'
coins: []
